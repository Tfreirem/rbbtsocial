// Este arquivo está sendo mantido para compatibilidade,
// mas não contém mais dados simulados de insights.
// Os dados reais são buscados diretamente do Supabase.

// Array vazio de insights
export const insightsData: any[] = [];

// Função para filtrar insights - mantida para compatibilidade
export const filtrarInsightsPorCategoria = (categoria: string) => {
  return [];
};

// Função para obter insights relacionados - mantida para compatibilidade 
export const obterInsightsRelacionados = (insightId: string, maxResults: number = 3) => {
  return [];
};

// Função para gerar novos insights - mantida para compatibilidade
export const gerarNovoInsight = () => {
  return null;
};

// Dados de cruzamento - mantidos para compatibilidade
export const cruzamentoDadosAdsOrganico = {
  correlacaoEngajamentoConversao: [],
  impactoConteudoOrganico: [],
  jornada_usuario: []
}; 